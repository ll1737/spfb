# 智域真实应用化改造计划

## 目标

把当前“功能原型”推进为可真实注册、登录、管理账号、接入真实 Worker、创建/排期/执行/重试发布任务，并可在 Electron 桌面端独立启动和打包的应用；保留现有用户修改，不引入测试数据作为业务结果。

## 阶段

- [complete] 1. 固定当前快照、方案和真实运行环境
- [complete] 2. 建立安全的真实持久化与认证边界
- [complete] 3. 补齐真实任务队列、排期、重试和状态持久化
- [complete] 4. 统一真实 Worker 适配器与平台能力声明
- [complete] 5. 接通前端真实功能并移除默认/测试数据路径
- [in_progress] 6. 完善 Electron 桌面启动、打包和本地 Worker 管理
- [in_progress] 7. 用 Python 3.12、Node 构建和端到端 API 检查验证

## 约束

- 用户明确授权本次修改项目；保留并整合当前已有修改，不做 reset/checkout/覆盖式回滚。
- 不把模拟发布结果当真实成功；未实现的平台返回明确的“未配置/不支持”，不伪造 URL。
- Python Worker 使用 `D:\python312\python.exe` 验证；不使用系统 Python 3.7 代替。
- 每个行为修复先补失败测试，再写最小实现；配置和打包文件按风险比例验证。
- 所有关键发现、决策和错误写入 findings.md / progress.md。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| PowerShell 插值字符串中 `$path:` 被解析为无效变量引用 | 1 | 后续改用 `${path}` 形式输出范围标题 |
| PowerShell 正则字符串包含引号时发生解析错误 | 1 | 改用更简单的逐文件检查命令，避免嵌套引号 |
| `py -3.12 -m pytest -q` 找不到已安装 Python | 1 | 记录为环境阻塞，改查 `python` 命令与静态代码证据，不重复同一命令 |
| PowerShell `foreach` 管道写法在对象构造表达式中产生空管道错误 | 1 | 先把循环结果赋值给数组，再单独格式化输出 |
| 启动构建后的服务做接口验证时 3000 端口已被占用 | 1 | 不终止未知进程，改为识别占用进程并对现有服务做只读请求 |
| PowerShell 对 `foreach` 输出直接接管道再次报空管道错误 | 1 | 后续不在同一语句末尾直接管道，改用数组变量收集结果 |
| PowerShell 目标清理检查再次在 `foreach` 后直接管道导致解析错误 | 1 | 改用数组变量后重新执行检查 |
| 本轮用户要求从分析转入真实应用化改造 | 1 | 以新的实现阶段替换已完成的分析阶段，保留历史发现作为依据 |
| `npm install --package-lock-only` 默认 npm cache 目录无写权限 | 1 | 改用工作区内的 `npm_config_cache` 重试，避免写入受限用户目录 |
| `electron-builder --dir` 在默认 `release/win-unpacked` 重命名时遇到 EPERM | 1 | 不删除未知锁定目录，改用独立 `release-verify` 输出目录重试 |
| 独立 `release-verify` 输出目录仍在 Electron 解压重命名阶段 EPERM | 2 | 记录为桌面打包环境权限/文件锁问题，下一步尝试受控提权验证，不重复普通命令 |
| 清理 Electron Builder 临时目录时 `default_app.asar` 被进程占用 | 1 | 已清理大部分临时目录，保留被锁目录，不强制终止未知进程 |
